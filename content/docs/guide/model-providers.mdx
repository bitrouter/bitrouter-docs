---
title: Model Providers
description: Add built-in and custom OpenAI, Anthropic, and Google-compatible providers.
---

import { Callout } from 'fumadocs-ui/components/callout';

## Built-in providers

BitRouter ships with built-in definitions for these providers:

| Provider | Protocol | Env prefix | Auto-detected key |
|----------|----------|------------|-------------------|
| `openai` | OpenAI | `OPENAI` | `OPENAI_API_KEY` |
| `anthropic` | Anthropic | `ANTHROPIC` | `ANTHROPIC_API_KEY` |
| `google` | Google | `GOOGLE` | `GOOGLE_API_KEY` |
| `openrouter` | OpenAI | `OPENROUTER` | `OPENROUTER_API_KEY` |

Built-in providers include a full model catalog with metadata (context length, modalities) and token pricing. You only need to supply an API key to start using them.

## Configure a provider

Add your API key in `bitrouter.yaml` — all other fields are inherited from the built-in definition:

```yaml
providers:
  openai:
    api_key: ${OPENAI_API_KEY}
  anthropic:
    api_key: ${ANTHROPIC_API_KEY}
```

Or skip the config file entirely and export the key in your environment:

```bash
export OPENAI_API_KEY=sk-...
bitrouter serve
# Use "openai:gpt-4o" as the model name
```

## Add a custom provider

Any OpenAI-compatible or Anthropic-compatible API works with the `derives` field:

```yaml
providers:
  my-company:
    derives: openai
    api_base: "https://api.mycompany.com/v1"
    api_key: "${MY_COMPANY_API_KEY}"

  moonshot:
    derives: anthropic
    api_base: "https://api.moonshot.ai/anthropic"
    api_key: "${MOONSHOT_API_KEY}"
```

The `derives` field inherits the protocol, model catalog, and all defaults from the named built-in. Only the fields you set are overridden.

## Provider fields

| Field | Description |
|-------|-------------|
| `derives` | Inherit from another provider definition |
| `api_protocol` | `openai`, `anthropic`, or `google` |
| `api_base` | Base URL for the upstream API |
| `api_key` | Default API key |
| `env_prefix` | Auto-load `{PREFIX}_API_KEY` and `{PREFIX}_BASE_URL` from environment |
| `default_headers` | Extra HTTP headers sent with every request |
| `auth` | Authentication method override |
| `models` | Per-model metadata and pricing catalog |

### Auth modes

```yaml
# Default: Bearer token (used automatically when api_key is set)
providers:
  example-bearer:
    api_key: "sk-..."

# Custom header (e.g. x-api-key)
providers:
  example-header:
    auth:
      type: header
      header_name: "x-api-key"
      api_key: "key-..."

# Custom auth (e.g. for x402 or SIWx)
providers:
  example-custom:
    auth:
      type: custom
      method: siwx
      params:
        chain_id: 1
```

## Model routing

Define virtual model names that route to one or more provider endpoints:

```yaml
models:
  default:
    strategy: priority
    endpoints:
      - provider: openai
        model_id: gpt-4o
      - provider: anthropic
        model_id: claude-sonnet-4-20250514

  fast:
    strategy: load_balance
    endpoints:
      - provider: openai
        model_id: gpt-4o-mini
        api_key: "sk-key-a"
      - provider: openai
        model_id: gpt-4o-mini
        api_key: "sk-key-b"
```

### Strategies

| Strategy | Behavior |
|----------|----------|
| `priority` | Try endpoints in order; failover to the next on error |
| `load_balance` | Distribute requests evenly via round-robin |

### Per-endpoint overrides

Each endpoint can override `api_key` and `api_base` independently, useful for spreading load across multiple API keys.

## Direct routing

Skip the `models` section and use `provider:model_id` syntax directly:

```bash
curl http://localhost:8787/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model": "openai:gpt-4o", "messages": [{"role": "user", "content": "Hello"}]}'
```

This works for any configured provider without defining a model alias.

## Dynamic routes

Add or remove routes at runtime without restarting:

```bash
# List all routes
bitrouter route list

# Add a route
bitrouter route add research openai:o3 anthropic:claude-sonnet-4-20250514 --strategy priority

# Remove a route
bitrouter route rm research
```

Dynamic routes are stored in memory and reset on restart. Config-defined routes are always loaded.
