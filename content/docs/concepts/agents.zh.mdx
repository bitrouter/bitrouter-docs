---
title: 智能体（Agents）
description: BitRouter 的 agent-native 含义——ACP 提供身份与任务派发，KYA 身份让自主 agent 按次付费，Agent Skills 教会 agent 使用 BitRouter。
---

BitRouter 是 **agent-native（以智能体为中心）**的：下面这些原语都假定调用方是自主 agent，而非键盘前的人。这体现在三处——agent 如何被识别与触达、如何付费，以及如何学会使用路由器。

## 身份与派发 —— ACP

正如 MCP 网关让 agent 触达众多工具服务器，**ACP 网关**负责 agent 这一侧：跨 host 的 **agent 身份、发现与任务派发**。它让 agent 在网络中拥有一席之地、可被发现、并能交接或接收任务——采用 BitRouter 处处一致的单端点模型。

## 可验证、能付费的身份 —— KYA

一个持有你密钥的自主 agent，若没有自身的身份，便是一种隐患。**KYA（Know-Your-Agent）**赋予 agent 一个**可验证的身份**，这正是自主付费得以安全的前提：凭借该身份，agent 可以通过机器支付协议（x402/MPP）**按次付费**，自行结算每个请求，全程无需信用卡、预付额度或发票。

## 知道如何使用 BitRouter —— Skills

最后，agent 还得真正会驱动路由器。**Agent Skills** 是即插即用的能力，**教会 agent 使用 BitRouter**——如何安装它、把运行时指向它、并通过它调用模型与工具——让这些知识随 agent 一同迁移，而不是留在某个人的配置笔记里。

## 了解如何使用

- [ACP 网关](/docs/features/acp) —— 跨 host 的 agent 身份、发现与任务派发。
- [Agent Skills](/docs/features/agentskills) —— 教会 agent 使用 BitRouter 的即插即用技能。
- [智能体支付](/docs/cloud/payment) —— 通过 MPP / x402 实现自主按次付费。
