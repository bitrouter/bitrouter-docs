---
title: 高级用法
description: Python SDK 高级用法 — 本地开发、错误处理、自定义 httpx、最佳实践
---

# 高级用法

## 本地开发

当针对需要验证生产域名的本地服务器进行开发时：

```python
async with AimoClient(AimoClientOptions(
    signer=signer,
    base_url="http://localhost:8000",
    siwx_domain="beta.aimo.network",
)) as client:
    balance = await client.session_balance()
```

这同样适用于 OpenAI provider：

```python
client = create_openai_client(
    signer=signer,
    base_url="http://localhost:8000",
    siwx_domain="beta.aimo.network",
)
```

## 错误处理

`chat_completions` 返回 `httpx.Response`，因此在解析前请检查状态码：

```python
try:
    response = await client.chat_completions({
        "model": "openai/gpt-4o",
        "messages": [{"role": "user", "content": "Hello!"}],
    })

    if response.status_code != 200:
        error = response.json()
        print(f"API error: {error.get('error')}")
    else:
        data = response.json()
        print(data["choices"][0]["message"]["content"])

except Exception as e:
    print(f"Request failed: {e}")
```

## 多轮对话

```python
messages = [
    {"role": "system", "content": "You are a helpful assistant."},
    {"role": "user", "content": "What is AiMo Network?"},
]

response1 = await client.chat_completions({
    "model": "openai/gpt-4o",
    "messages": messages,
})

data1 = response1.json()
messages.append(data1["choices"][0]["message"])

# 继续对话
messages.append({"role": "user", "content": "How does it work?"})

response2 = await client.chat_completions({
    "model": "openai/gpt-4o",
    "messages": messages,
})
```

## 使用不同的模型

所有模型通过 OpenAI 兼容的端点访问：

```python
models = ["openai/gpt-4o", "anthropic/claude-sonnet-4", "google/gemini-2.0-flash"]

for model in models:
    response = await client.chat_completions({
        "model": model,
        "messages": [{"role": "user", "content": "Hello!"}],
    })
    data = response.json()
    print(f"{model}: {data['choices'][0]['message']['content']}")
```

## 最佳实践

### 安全性

```python
import os

# 从环境变量加载密钥
private_key = os.environ["SOLANA_PRIVATE_KEY"]

# 切勿在源代码中硬编码私钥
```

### 性能

```python
# 复用客户端实例 — SIWx 会话会被缓存
async with AimoClient(options) as client:
    # 跨请求复用缓存的 SIWx 会话
    await client.chat_completions({...})
    await client.chat_completions({...})

# 不要为每个请求创建新的客户端
```

### 自定义 httpx 客户端

传入自定义的 httpx `AsyncClient` 以精细控制超时、连接池和其他 transport 设置：

```python
import httpx

custom_httpx = httpx.AsyncClient(
    timeout=60.0,
    limits=httpx.Limits(max_connections=10),
)

async with AimoClient(AimoClientOptions(
    signer=signer,
    base_url="https://beta.aimo.network",
    httpx_client=custom_httpx,
)) as client:
    await client.chat_completions({...})
```
