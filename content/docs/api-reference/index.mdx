---
title: API Reference
description: REST API documentation for the BitRouter routing proxy.
---

import { Callout } from 'fumadocs-ui/components/callout';

<Callout type="warn">
The BitRouter API is in **early beta** and under active development. Endpoints, request/response formats, and behavior may change without notice. Pin to a specific version and watch the changelog for breaking changes.
</Callout>

The BitRouter API is a modular LLM routing proxy that exposes multiple provider-compatible API surfaces through a single gateway. Route requests to OpenAI, Anthropic, and Google models using the native API format of each provider.

## Base URL

```
https://api.bitrouter.ai/v1
```

## Multi-Protocol Routing

BitRouter accepts requests in the native format of each supported provider. The router resolves the requested model name to a configured upstream provider, forwards the request, and returns the response in the same provider format.

## Endpoints

| Section | Description |
|---|---|
| [OpenAI Compatible](/docs/api-reference/openai) | Chat completions and Responses API |
| [Anthropic Compatible](/docs/api-reference/anthropic) | Messages API |
| [Google Compatible](/docs/api-reference/google) | Generative AI (generateContent) |
| [Discovery](/docs/api-reference/discovery) | Model and route listing |
| [Admin](/docs/api-reference/admin) | Runtime route management |
| [Observability](/docs/api-reference/observability) | Health check and metrics |
