---
title: 高级用法
description: TypeScript SDK 高级用法 — 流式传输、本地开发、自定义 fetch、错误处理
---

# 高级用法

## 流式传输

SDK 通过 `chatCompletions` 返回的原始 `Response` 支持 OpenAI 兼容的 SSE 流式传输：

```typescript
const response = await client.chatCompletions({
  model: "openai/gpt-4o",
  messages: [{ role: "user", content: "Tell me a story" }],
  stream: true,
});

const reader = response.body!.getReader();
const decoder = new TextDecoder();

while (true) {
  const { done, value } = await reader.read();
  if (done) break;

  const chunk = decoder.decode(value);
  const lines = chunk.split("\n").filter((line) => line.trim());

  for (const line of lines) {
    if (line.startsWith("data: ") && line.slice(6) !== "[DONE]") {
      const data = JSON.parse(line.slice(6));
      const content = data.choices[0]?.delta?.content;
      if (content) {
        process.stdout.write(content);
      }
    }
  }
}
```

如需更高级的流式 API，请使用 [`@bitrouter/provider`](/docs/sdk-reference/ts-provider) 配合 Vercel AI SDK 的 `streamText`。

## 本地开发

当针对需要验证生产域名的本地服务器进行开发时：

```typescript
const client = new BitRouterClient({
  signer,
  baseUrl: "http://localhost:8000",
  siwxDomain: "beta.bitrouter.ai",  // 覆盖签名域名
});
```

这同样适用于 provider：

```typescript
const br =bitrouter({
  signer,
  baseURL: "http://localhost:8000",
  siwxDomain: "beta.bitrouter.ai",
});
```

## 自定义 Fetch 拦截器

通过提供 `fetchOverride` 添加自定义的请求/响应处理：

```typescript
const customFetch: typeof fetch = async (url, init) => {
  console.log(`Request: ${init?.method} ${url}`);
  const response = await fetch(url, init);
  console.log(`Response: ${response.status}`);
  return response;
};

const client = new BitRouterClient({
  signer,
  baseUrl: "https://beta.bitrouter.ai",
  fetchOverride: customFetch,
});
```

## 错误处理

`chatCompletions` 返回原始 `Response`，因此在解析前请检查 `response.ok`：

```typescript
try {
  const response = await client.chatCompletions({
    model: "openai/gpt-4o",
    messages: [{ role: "user", content: "Hello!" }],
  });

  if (!response.ok) {
    const error = await response.json();
    console.error(`API error: ${error.error}`);
  } else {
    const data = await response.json();
    console.log(data.choices[0].message.content);
  }
} catch (error) {
  console.error(`Request failed: ${error.message}`);
}
```

## 多轮对话

```typescript
const messages = [
  { role: "system", content: "You are a helpful assistant." },
  { role: "user", content: "What is BitRouter?" },
];

const response1 = await client.chatCompletions({
  model: "openai/gpt-4o",
  messages,
});

const data1 = await response1.json();
messages.push(data1.choices[0].message);

// 继续对话
messages.push({ role: "user", content: "How does it work?" });

const response2 = await client.chatCompletions({
  model: "openai/gpt-4o",
  messages,
});
```

## 最佳实践

### 安全性

```typescript
// 从环境变量加载密钥
const privateKey = process.env.SOLANA_PRIVATE_KEY;

// 切勿在源代码中硬编码私钥
```

### 性能

```typescript
// 复用客户端实例 — SIWx 会话缓存 1 小时
const client = new BitRouterClient({ signer, baseUrl: "https://beta.bitrouter.ai" });

await client.chatCompletions({ ... });
await client.chatCompletions({ ... }); // 复用缓存的 SIWx 会话
```

### 使用不同的模型

所有模型通过 OpenAI 兼容的端点访问：

```typescript
// OpenAI
await client.chatCompletions({
  model: "openai/gpt-4o",
  messages: [{ role: "user", content: "Hello!" }],
});

// Anthropic
await client.chatCompletions({
  model: "anthropic/claude-sonnet-4",
  messages: [{ role: "user", content: "Hello!" }],
});

// Google
await client.chatCompletions({
  model: "google/gemini-2.0-flash",
  messages: [{ role: "user", content: "Hello!" }],
});
```
