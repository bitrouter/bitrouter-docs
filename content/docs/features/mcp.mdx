---
title: MCP gateway
description: Proxy MCP servers so agents discover and call tools across hosts through one gateway.
---

{/* TODO: rule syntax, built-in classifiers, custom predicates, response actions. */}
