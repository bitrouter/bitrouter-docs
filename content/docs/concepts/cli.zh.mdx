---
title: CLI
description: 运行 BitRouter 的单个本地二进制——一个供你运行时指向的端点、一个由你掌控的守护进程，以及一个可脚本化的账户操作面。
---

BitRouter 以单个**静态二进制** `bitrouter` 的形式发布，无需安装任何依赖。它扮演两个角色：既运行你 agent 所对话的**本地路由器**，又是你托管账户的**命令行操作面**。

## 本地端点

你的 agent 从不直接与远程 API 对话——它指向本地运行的二进制，默认地址为 `http://127.0.0.1:4356`。本文档中的其余一切——四种模型协议、MCP 与 ACP 网关——都从这一个端点对外提供。

你以守护进程的形式运行它，并掌控其生命周期：

- `bitrouter serve` —— 在前台运行路由器。
- `bitrouter start` / `stop` / `status` —— 以后台守护进程方式管理它。

## 账户命令

同一个二进制也管理你的托管账户，因此脚本和 agent 能完成 Console 所能做的一切：

- `bitrouter auth login | whoami | logout` —— 登录你的账户（设备码 OAuth），并查看或清除本地凭证。
- `bitrouter cloud …` —— 管理账户本身：API 密钥、用量与请求历史、计费余额、BYOK 供应商密钥、路由与预算策略，以及 OAuth 客户端。

完整的命令清单、参数与输出格式，见 [CLI 参考](/docs/reference/cli)。
