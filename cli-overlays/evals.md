---
title: Evaluation and trajectories
---

`eval` is the evaluator-neutral evidence exchange: seal subjects, admit typed
results, and freeze content-addressed snapshots. `trajectory` inspects and
replays the daemon's durable request history. These are lower-level evidence
surfaces; `bitrouter optimize` composes them into the guided workflow-quality
and cost experiment.
