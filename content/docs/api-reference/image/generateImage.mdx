---
title: Generate images
full: true
_openapi:
  method: POST
  route: /image/generate
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >
          Generate one or more images from a text prompt.


          Two output modes are available:

          - `output_type: "data_url"` (default) — Returns JSON with
          base64-encoded data URLs. Supports multiple images (`n >= 1`).

          - `output_type: "file"` — Returns the raw binary file directly. Only
          supports a single output (`n = 1`).
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

Generate one or more images from a text prompt.

Two output modes are available:
- `output_type: "data_url"` (default) — Returns JSON with base64-encoded data URLs. Supports multiple images (`n >= 1`).
- `output_type: "file"` — Returns the raw binary file directly. Only supports a single output (`n = 1`).


<APIPage document={"./openapi.yaml"} operations={[{"path":"/image/generate","method":"post"}]} webhooks={[]} hasHead={false} />