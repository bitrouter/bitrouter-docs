---
title: 模型提供商
description: 添加内置和自定义的 OpenAI、Anthropic 和 Google 兼容提供商。
---

import { Callout } from 'fumadocs-ui/components/callout';

## 内置提供商

BitRouter 内置以下提供商定义：

| 提供商 | 协议 | 环境前缀 | 自动检测的密钥 |
|--------|------|----------|----------------|
| `openai` | OpenAI | `OPENAI` | `OPENAI_API_KEY` |
| `anthropic` | Anthropic | `ANTHROPIC` | `ANTHROPIC_API_KEY` |
| `google` | Google | `GOOGLE` | `GOOGLE_API_KEY` |
| `openrouter` | OpenAI | `OPENROUTER` | `OPENROUTER_API_KEY` |

内置提供商包含完整的模型目录（上下文长度、模态）和 token 定价。你只需提供 API 密钥即可开始使用。

## 配置提供商

在 `bitrouter.yaml` 中添加 API 密钥 — 其他字段从内置定义继承：

```yaml
providers:
  openai:
    api_key: ${OPENAI_API_KEY}
  anthropic:
    api_key: ${ANTHROPIC_API_KEY}
```

或者跳过配置文件，直接在环境中导出密钥：

```bash
export OPENAI_API_KEY=sk-...
bitrouter serve
# 使用 "openai:gpt-4o" 作为模型名称
```

## 添加自定义提供商

任何 OpenAI 兼容或 Anthropic 兼容的 API 都可以通过 `derives` 字段接入：

```yaml
providers:
  my-company:
    derives: openai
    api_base: "https://api.mycompany.com/v1"
    api_key: "${MY_COMPANY_API_KEY}"

  moonshot:
    derives: anthropic
    api_base: "https://api.moonshot.ai/anthropic"
    api_key: "${MOONSHOT_API_KEY}"
```

`derives` 字段从指定的内置提供商继承协议、模型目录和所有默认值。只有你设置的字段会被覆盖。

## 提供商字段

| 字段 | 描述 |
|------|------|
| `derives` | 从另一个提供商定义继承 |
| `api_protocol` | `openai`、`anthropic` 或 `google` |
| `api_base` | 上游 API 的基础 URL |
| `api_key` | 默认 API 密钥 |
| `env_prefix` | 从环境自动加载 `{PREFIX}_API_KEY` 和 `{PREFIX}_BASE_URL` |
| `default_headers` | 每个请求附带的额外 HTTP 头 |
| `auth` | 认证方式覆盖 |
| `models` | 每个模型的元数据和定价目录 |

### 认证模式

```yaml
# 默认：Bearer token（设置 api_key 时自动使用）
providers:
  example-bearer:
    api_key: "sk-..."

# 自定义头（如 x-api-key）
providers:
  example-header:
    auth:
      type: header
      header_name: "x-api-key"
      api_key: "key-..."

# 自定义认证（如 x402 或 SIWx）
providers:
  example-custom:
    auth:
      type: custom
      method: siwx
      params:
        chain_id: 1
```

## 模型路由

定义虚拟模型名称，路由到一个或多个提供商端点：

```yaml
models:
  default:
    strategy: priority
    endpoints:
      - provider: openai
        model_id: gpt-4o
      - provider: anthropic
        model_id: claude-sonnet-4-20250514

  fast:
    strategy: load_balance
    endpoints:
      - provider: openai
        model_id: gpt-4o-mini
        api_key: "sk-key-a"
      - provider: openai
        model_id: gpt-4o-mini
        api_key: "sk-key-b"
```

### 策略

| 策略 | 行为 |
|------|------|
| `priority` | 按顺序尝试端点；出错时故障转移到下一个 |
| `load_balance` | 通过轮询均匀分配请求 |

### 每端点覆盖

每个端点可以独立覆盖 `api_key` 和 `api_base`，适用于在多个 API 密钥之间分散负载。

## 直接路由

跳过 `models` 部分，直接使用 `provider:model_id` 语法：

```bash
curl http://localhost:8787/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{"model": "openai:gpt-4o", "messages": [{"role": "user", "content": "Hello"}]}'
```

这适用于任何已配置的提供商，无需定义模型别名。

## 动态路由

在运行时添加或移除路由，无需重启：

```bash
# 列出所有路由
bitrouter route list

# 添加路由
bitrouter route add research openai:o3 anthropic:claude-sonnet-4-20250514 --strategy priority

# 移除路由
bitrouter route rm research
```

动态路由存储在内存中，重启后重置。配置文件中定义的路由始终会被加载。
